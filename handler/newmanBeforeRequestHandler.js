var handler = {
    handle: function(err, args, config) {
        // var str = '正在执行: ' + args.request.body.raw;
        // if (err) {
        //     str = str + " 报错, " + err
        // }
        // console.info(str)
    }

}

module.exports = handler